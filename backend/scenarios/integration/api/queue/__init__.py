"""API queue endpoint tests for Vedro scenarios.

Tests the /api/queue endpoints for queue status, active items, statistics,
and individual queue item retrieval.

Example:
    >>> vedro run scenarios/integration/api/queue/
"""
